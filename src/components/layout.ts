/** Shared shell layout tokens — keep sidebar brand block and page header aligned. */
export const ADMIN_SHELL_HEADER_HEIGHT =
  "min-h-[88px]";

export const ADMIN_SHELL_HEADER =
  `${ADMIN_SHELL_HEADER_HEIGHT} flex w-full shrink-0 items-center border-b border-border`;

/** Horizontal inset for header and main content — must stay in sync. */
export const ADMIN_SHELL_CONTENT_PADDING =
  "px-4 sm:px-6 lg:px-8";
